name: "service-a"

deployment:
  replicaCount: 1

  image:
    image: offerfit-service-a
    tag: 8817095

  env:
    - name: "APP_PORT"
      value: 8011

service:
  port: 8011

ingress:
  enabled: true
